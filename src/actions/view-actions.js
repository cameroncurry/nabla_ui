import * as types from '../action-types'

export const qtAccessDialogShow = () => ({
  type: types.QT_ACCESS_DIALOG_SHOW
})

export const qtAccessDialogHide = () => ({
  type: types.QT_ACCESS_DIALOG_HIDE
})
